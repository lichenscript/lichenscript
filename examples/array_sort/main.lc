
function main() {
    const arr = [3, 2, 1, 4, 5];
    arr.sort((a: i32, b: i32): i32 => {
        a - b
    });

    let index = 0;
    while index < arr.length {
        print(arr[index]);
        index += 1;
    }
}
