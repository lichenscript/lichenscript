
function createTuple(): (string, i32) {
    ("a", 1)
}

function main() {
    const tuple = createTuple();
    print(tuple);

    match tuple {
        case (a, b) => print("a:", a, "b:", b)
    }
}
