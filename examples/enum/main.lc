

enum MyError {

    case MyError1(i32, i32, i32)

}

function main() {
    let a = Some(1);
    print(a);
    print(None);
    match a {
        case Some(i) => {
            print("Some")
        }
        case None => {
            print("None")
        }
    }
    print(a.isSome());
    print(a.isNone());
    print(MyError1(0, 0, 0));
}
