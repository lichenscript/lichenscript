
function main() {
    let a = #{
        "name": "Vincent Chan"
    };
    let test = a.get("name");
    match test {
        case Some(name) => print("found: ", name)
        case None => print("not found")
    }
}
